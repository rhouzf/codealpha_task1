#include<stdio.h>
#include<stdio.h>
int main(){
	int pid, int cr;
	switch( pid=fork()){
		case 1: print;
			beak;
		case 0: //fils
			{
	    		exit(0);
			}
		default { wait(& cr);}
	}
	if (!cr)
		exit(0);
	else 
		exit(5);
}
